# IntellMeet React frontend

## Setup

```bash
cp .env.example .env
npm install
npm run dev
```

The frontend uses `VITE_API_URL` from `.env`:

```env
VITE_API_URL=http://localhost:5001/api
```

## Checks

```bash
npm run lint
npm run build
```
